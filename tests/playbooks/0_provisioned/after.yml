# Assert that the vms are available and provisionned
- hosts: localhost
  tasks:
    - jclaveau.vagrant.ssh_config:
        name: srv001
        vagrant_root: '.'
      register: ssh_config

    - debug:
        var: ssh_config

# - hosts: all
#   become: yes
#   tasks:
    # - debug:
    #     var: hostvars
    # - name: List peer status
    #   shell: "gluster peer status"
    #   register: peers_hostnames
    # - debug:
    #     var: peers_hostnames